# 权限系统架构与开发计划

## 术语约定

- PL：Player，指玩家主体与玩家级权限。
- PC：Player Character，指由 PL 所操控的角色或对象。
- GM：Game Master，指全局主持与裁决主体。
- OB：Observer，指默认只读的观察主体。

本文档中凡是“玩家权限”“玩家主体”“玩家视角”一律使用 PL，不再用 PC 表示玩家。

## 1. 目标

ElysianVTT 当前的核心问题不是“缺少一个按钮级别的权限开关”，而是整个控制链路还没有形成真正的服务端权威。现状里，客户端可以自报身份、直接发送意图、前端也能看到并操作一部分本不该默认开放的能力，这会导致三个后果：

1. 身份可以伪造，任何人都可能冒充 GM、PL 或其他实体控制者。
2. 权限边界不清，场景、引擎、实体、日志、骰子改值这些高风险操作没有统一裁决点。
3. 可见性和控制混在一起，前端一旦表现不一致，就会出现“看见了但不该看见”“能点但不该点”的问题。

因此，本项目的权限系统不应被理解为单一模块，而应被设计为一条横跨网络、业务、可见性、审计、前端体验的基础设施链路。它的目标是：

- GM 拥有全域控制权，能够管理场景、引擎、实体、日志、骰子、角色授权等所有方面。
- PL 仅默认控制自己的 PC，只能看到和操作与自身直接相关的内容；额外能力必须由 GM 授权。
- OB 作为独立的观察者角色存在，默认只读，权限与 PL 不同。
- 任何高风险行为都必须可审计、可追踪、可回放。
- 客户端永远不能作为安全边界，所有最终判断都必须发生在服务端。

## 2. 设计原则

### 2.1 服务端权威

所有权限判断、身份确认和可见性过滤都应由服务端完成。前端可以做按钮显隐、交互禁用、提示文案，但不能承担真正的安全职责。

原因很直接：只要最终裁决在客户端，就一定可以被绕过。权限系统的存在意义，就是让“绕过 UI”不再等于“绕过规则”。

### 2.2 身份、授权、可见性、审计分离

这四个问题必须拆开：

- 身份认证解决“你是谁”。
- 授权解决“你能做什么”。
- 可见性解决“你能看到什么”。
- 审计解决“发生了什么、是谁做的”。

如果把这些概念混在一起，后续会在日志过滤、实体控制、回放系统和权限撤销上反复返工。

### 2.3 最小权限原则

默认只给最少的能力：

- PL 默认只能控制自己的 PC。
- OB 默认只能观察。
- 任意跨角色、跨实体、跨场景的能力都必须显式授予。

这样做的原因是，权限系统最怕的不是功能不够，而是默认开得太大。一旦默认权限过宽，后续只会越来越难收口。

### 2.4 可组合的权限表达

本项目不适合只做一个简单的角色枚举。因为“谁能做什么”不仅取决于角色，还取决于资源范围和上下文条件。

例如：

- PL 只能控制自己的 PC。
- GM 可以临时授权某个 PL 控制某个盟友对应的 PC。
- 某个日志只有 GM 可见。
- 某个骰子只有结算阶段可以改值。

这类规则不能只靠 GM/PL/OB 三个枚举表达完，因此需要一个可组合的权限模型。

## 3. 主体模型

### 3.1 GM

GM 是全局最高权限主体。它的定义不是“权限更多的玩家”，而是“拥有管理规则、状态和授权的控制者”。

GM 应具备的能力：

- 启动、暂停、恢复、销毁场景和引擎。
- 控制任何实体，无论死活。
- 读取全部日志，包括 GM 日志与审计日志。
- 授予、撤销、转移、临时扩展其他人的权限。
- 修改骰子结果，但必须进入审计。
- 查看和操作被隐藏的信息。

选择将 GM 定义为全域主体的原因是：GM 的业务语义本身就是“裁决者”和“导演”。如果把 GM 也限制成普通角色的子类型，就会在系统里不断出现不必要的例外规则。

### 3.2 PL

PL 是普通参与者。PL 的核心特征是“默认只管自己所操控的 PC”。

PL 默认能力：

- 查看自己的角色和当前状态。
- 对自己的角色发起移动、施法、交互。
- 读取与自己相关的战斗信息和日志。
- 在 GM 明确授权后，操作额外的实体或资源。

PL 不应默认获得：

- 读取全部日志。
- 控制他人的角色。
- 修改骰子结果。
- 管理他人权限。
- 查看 GM 专属隐藏信息。

选择这种定义的原因是：PL 的核心体验是“以自己所操控的 PC 为中心参与游戏”，而不是拥有系统管理权。

### 3.3 OB

OB 建议作为独立角色，而不是“无权限 PL”。

原因是 OB 的业务语义不是“玩家但没有控制权”，而是“观察者”。它在默认能力、可见范围和交互体验上都与 PL 不同：

- OB 主要用于旁观、观战、直播、裁判席、测试席等场景。
- OB 默认只读，不应出现控制角色的按钮。
- OB 的可见性可能比 PL 更窄，也可能在某些规则下比 PL 更宽，但这取决于 GM 授权，不应默认复用 PL 逻辑。

如果把 OB 当成“没有权限的 PL”，后续会出现三个问题：

1. 前端会出现一堆“看得到但不能点”的 PL 交互。
2. 后端要不断判断“这个 PL 其实是 OB”。
3. 日志和视图过滤会变得模糊，难以表达观察者的特殊规则。

因此推荐将 OB 单独建模，再通过授权系统决定它是否能临时获得某些能力。

## 4. 权限域设计

权限不应该只按“角色”划分，还应该按资源域划分。建议至少拆成以下六个域。

### 4.1 场景域

控制对象：场景本身。

典型能力：

- 进入场景。
- 离开场景。
- 创建场景。
- 暂停场景。
- 恢复场景。
- 销毁场景。

推荐规则：

- GM 拥有全部场景域权限。
- PL 仅能加入被允许的场景。
- OB 仅能进入被允许的观战场景。

替代方案：
- 仅用 sceneId 直接进场，不做权限控制。
- 用房间码控制场景进入。
- 用场景 ACL 控制进入与操作。

推荐当前方案的原因：房间码只能解决“谁能进”，不能解决“进来后能看什么、能做什么”。ACL 可扩展性最好，因此更适合作为最终落点。

### 4.2 引擎域

控制对象：CombatEngine、ExploreEngine 等运行实例。

典型能力：

- 启动。
- 停止。
- 挂载实体。
- 卸载实体。
- 冻结。
- 恢复。
- 重置。

推荐规则：

- GM 可完全管理引擎。
- 普通玩家无权直接启停引擎。
- 观战者无权影响引擎状态。

替代方案：
- 引擎启动完全自动化，由场景进入触发。
- 引擎由单独管理后台控制。
- 引擎操作全都通过 GM 指令接口完成。

推荐当前方案的原因：引擎是游戏规则执行器，必须与场景管理和权限系统联动；只靠自动化会缺乏人工裁决能力，只靠后台接口又会把流程割裂。

### 4.3 实体域

控制对象：角色、道具、召唤物、投射物、环境实体等。

典型能力：

- 查看实体。
- 选中实体。
- 控制实体移动。
- 对实体施法或交互。
- 修改实体状态。
- 在死亡后继续操作尸体或残骸。
- 临时接管他人实体。

推荐规则：

- PL 默认只能操作自己的 PC。
- GM 能操作任何实体，无论生死。
- OB 默认不控制任何实体。
- 被授权实体必须带作用域，不允许泛化成“所有实体”。

替代方案：
- 仅用“当前选中实体”决定权限。
- 用“owned entity”列表决定权限。
- 允许 GM 动态接管任何实体。

推荐当前方案的原因：仅靠当前选中态无法防止越权；仅靠 owned entity 也无法表达临时授权；GM 动态接管则必须是明确的管理能力，否则无法支撑主持和裁决。

### 4.4 日志域

控制对象：系统日志、战斗日志、审计日志、调试日志。

典型能力：

- 读取全部日志。
- 读取 GM 专属日志。
- 读取玩家可见战斗日志。
- 读取审计日志。
- 导出日志。

推荐规则：

- GM 读取全部日志。
- PL 只读取自己的可见日志与战斗日志。
- OB 只读取被允许的观察视图日志。
- 审计日志默认只对 GM 或审计角色可见。

替代方案：
- 所有人看到同一套日志。
- 仅根据日志等级区分，不根据身份区分。
- 只在前端隐藏日志条目。

推荐当前方案的原因：日志本身就是高敏感信息，前端隐藏无法防泄漏；身份分级才是正确做法。

### 4.5 骰子域

控制对象：掷骰结果、骰池、重投、爆炸骰、暴击标签、人工改值。

典型能力：

- 查看骰子结果。
- 提交重掷请求。
- 标记系统自动改值。
- 人工修改骰子结果。
- 解释改值原因。

推荐规则：

- 默认只有 GM 可人工改值。
- 系统规则允许自动改值，但必须标记来源。
- PL 不应默认拥有改值能力。
- 改值必须进入审计链。

替代方案：
- 完全禁止改值。
- 允许所有玩家改值。
- 只允许系统自动改值，不允许人工改值。

推荐当前方案的原因：完全禁止改值会削弱 GM 的叙事裁决能力；允许所有玩家改值会破坏公平性；系统自动改值保留了规则表达能力，而 GM 人工改值保留了主持弹性。

### 4.6 权限域

控制对象：其他人的权限本身。

典型能力：

- 授予权限。
- 撤销权限。
- 临时授权。
- 转移管理权。
- 提升或降级角色能力。

推荐规则：

- GM 拥有全部权限管理能力。
- 普通 PL 只能查看自己的权限状态。
- OB 默认不具备权限管理能力。

替代方案：
- 只允许 GM 手工管理。
- 允许房主管理局部权限。
- 权限委托给“会话管理员”。

推荐当前方案的原因：先把 GM 作为唯一全局授权者，能保证系统简单、可控、便于审计；房主或管理员可以作为后续扩展，而不是第一版就把系统做复杂。

## 5. 认证层设计

认证层解决的是“连接的人是谁”。推荐第一阶段使用自建 JWT，而不是直接接 OAuth。

### 5.1 为什么先选 JWT

JWT 的优点：

- 与 Socket.io 连接天然兼容。
- 部署简单，不依赖第三方身份提供商。
- 适合当前 monorepo 和单后端架构。
- 可以把 userId、role、sessionId、授权版本号等信息直接放进 token 或 token 解析结果里。

### 5.2 为什么不一开始就上 OAuth

OAuth 的优点是接第三方账号方便，但它会带来：

- 更多依赖。
- 更多配置项。
- 更复杂的登录流程。
- 更高的初期集成成本。

当前项目最紧迫的不是“接入外部账号”，而是“先把身份信任链修好”。因此 JWT 更适合作为首发方案。

### 5.3 会话流程建议

建议流程如下：

1. 用户先完成登录或拿到房间票据。
2. 服务端签发短期 access token。
3. 客户端连接 Socket 时携带 token。
4. 服务端校验 token，解析出身份上下文。
5. 服务端绑定 socket 与会话身份，后续所有意图都只认这个绑定结果。
6. token 过期后要求刷新或重新认证。

### 5.4 替代方案比较

- 仅用 cookie session：实现简单，但 WebSocket 场景下会话控制和跨端扩展较弱。
- JWT + refresh token：平衡开发成本和安全性，是当前推荐方案。
- OAuth + JWT：适合第二阶段做外部账号接入，但不应作为第一版主方案。

## 6. 授权层设计

授权层解决的是“你能做什么”。这里推荐 RBAC 与 ABAC 混合，而不是只靠单一角色表。

### 6.1 为什么不能只做 RBAC

RBAC 适合回答“你是什么角色”，但回答不了：

- 这个 PL 能不能控制某个被 GM 临时授权的实体？
- 这个动作是否只对当前场景有效？
- 这个日志是否只对当前身份可见？

这些都需要资源范围和上下文条件，因此不能只靠角色枚举。

### 6.2 为什么不能只做 ABAC

ABAC 很灵活，但如果从第一天就完全依赖属性规则，会出现：

- 规则过多。
- 难以维护。
- 难以为新成员理解。
- 容易写出重复和冲突策略。

因此更稳妥的方式是：

- RBAC 负责角色模板。
- ABAC 负责资源范围与上下文判断。

### 6.3 授权抽象建议

建议把授权拆成三个概念：

- Capability：能力，例如 move、cast、read_log、modify_roll。
- Scope：范围，例如 current_actor、owned_entities、current_scene、delegated_entities。
- Condition：条件，例如 role == GM、sceneId matches、entity is alive、permission not expired。

### 6.4 策略执行模型

不建议一开始就做 DSL，而是把 ABAC 收敛为可测试的策略函数，由统一的 Policy Service 负责执行入口。Capability、Scope、Condition 可以作为策略输入，但策略实现本身应保持强类型、可调试、可单元测试。

建议遵循以下原则：

- 业务规则优先放在代码中的 policy 函数里，避免一部分在代码、一部分在配置、一部分在数据库。
- 数据库只保存角色、授权、范围和过期信息，不保存可执行策略字符串。
- 相同 intent 只能经过同一条 policy 入口，避免不同模块各自判断。

### 6.5 权限快照

在会话绑定或授权变更时生成权限快照，用于高频判断和回放辅助。快照不是权限真相，而是可缓存的判定结果。

建议快照至少包含：

- controllableEntities。
- visibleEntities。
- capabilities。
- currentSceneIds。

权限变更时应对受影响范围做增量更新，而不是每次 intent 都从头计算全量关系。

### 6.6 决策优先级建议

建议优先级如下：

1. 明确拒绝。
2. GM 显式允许。
3. PL/OB 的默认角色权限。
4. 临时授权与委托。
5. 资源上下文和场景条件。

这样做的原因是，GM 的管理权必须可以覆盖普通规则，否则主持能力会被授权系统误伤。但“GM 覆盖一切”不等于可以覆盖系统历史和审计边界，后者必须单独约束。

### 6.7 GM 的不可越界区

建议明确以下系统级不可修改内容：

- 审计日志只追加，不允许删除。
- 原始骰子记录只保留原值，不允许覆盖。
- replay 数据不可直接改写，只能追加修正事件。
- 权限变更本身必须形成审计链，不能被静默吞掉。

### 6.8 替代方案

- 纯角色枚举：实现快，但扩展差。
- 纯 ACL：粒度细，但管理成本高。
- RBAC + ABAC：综合平衡最好，适合作为长期方案。

## 7. 网络层与意图路由

网络层必须承担第一道安全防线，而不是只是消息转发。

### 7.1 SocketServer 应做什么

SocketServer 应负责：

- 连接建立。
- 认证校验。
- 会话绑定。
- CORS 收敛。
- 断线与重连处理。
- 错误回包。

### 7.2 IntentRouter 应做什么

IntentRouter 应负责：

- 意图结构校验。
- 当前会话是否允许发出这个意图。
- 目标实体是否属于当前可控集合。
- 速率限制和基础幂等检查。

### 7.3 CombatEngine 应做什么

CombatEngine 不应该再判断“这个人有没有资格发这个意图”。它只应该执行经过认证和授权的合法输入。

这样拆分的原因是：如果把授权逻辑塞进引擎内部，未来每个系统都会重复写一套判断，最终导致规则不一致。正确的做法是把入口收紧，把引擎保持纯粹。

### 7.4 目前代码中的关键问题

当前实现里，客户端可以直接传 actorId，并通过 SocketServer 送入引擎。这意味着“谁发起控制”与“控制谁”还没有被服务端强绑定。

这必须改为：

- 客户端只提交操作意图。
- 服务端根据会话身份映射出可控实体。
- 如果目标不在授权范围内，立即拒绝。

### 7.5 替代方案

- 在引擎里做授权：不推荐，职责过重。
- 在 SocketServer 里一次性做完所有校验：可行但臃肿。
- SocketServer 负责认证，IntentRouter 负责授权，是当前更稳的分层。

### 7.6 Intent 标准化

建议统一所有控制请求的最小形态，让权限判断围绕同一个数据结构展开，而不是散落在各处。

```ts
interface Intent {
	type: string;
	actor?: string;
	target?: string;
	payload: unknown;
}
```

统一后的好处是：

- 权限判断入口更少。
- 单元测试更容易覆盖。
- 前后端对错误和拒绝路径更容易对齐。

### 7.7 失败路径与错误模型

拒绝路径必须和允许路径一样明确。建议统一错误模型，至少覆盖：

- UNAUTHENTICATED：未认证。
- UNAUTHORIZED：无权限。
- INVALID_TARGET：目标不合法或不在作用域内。
- PERMISSION_EXPIRED：授权已过期。
- AUDIT_REQUIRED：动作需要审计确认。

客户端在收到拒绝后应同步按钮态、提示原因，并避免对同一意图盲目重试。

## 8. 可见性与日志

可见性系统必须是服务端过滤，不是前端裁剪。

### 8.1 视图分层建议

- GM 视图：全量状态、全量视觉事件、全量日志。
- PL 视图：自己实体、授权实体、可见战斗信息。
- OB 视图：观察级别数据，默认不显示敏感细节。

### 8.2 为什么要独立可见性过滤

如果只在前端隐藏：

- 数据实际上仍然下发了。
- 浏览器控制台和网络抓包仍可能看到。
- 回放和审计不会一致。

因此必须在服务端广播前过滤。

### 8.3 日志系统的落地方式

Logger 目前已经有可见性字段，但还缺“按 viewer 过滤后再发送”的管道。建议把日志处理流程做成：

1. Logger 产生日志事件。
2. EventBus 或日志管道接收事件。
3. 按身份、场景、可见性过滤。
4. 分发给不同客户端。

### 8.4 替代方案

- 所有人收同一份日志：简单，但泄漏严重。
- 只在前端过滤：不可接受。
- 服务端分级广播：最安全，也最便于审计。

### 8.5 预分组广播

不要按用户逐个过滤后再广播，应该先按身份和可见性预分组，再向组广播。这样可以把高频广播从按用户遍历，收敛到按组分发。

建议至少分成以下组：

- GM 组。
- PL 组。
- OB 组。
- 临时授权组。

预分组应由权限快照驱动，这样可见性判断和广播分发可以共享同一份中间结果。

## 9. 骰子与审计

骰子系统是最容易引发争议的权限点之一，因此必须单独设计。

### 9.1 设计目标

- 允许 GM 进行人工裁决。
- 允许系统自动改值。
- 不允许 PL 随意修改结果。
- 所有改值都要留痕。

### 9.2 审计字段建议

每次改值建议记录：

- 操作者身份。
- 操作时间。
- 场景 id。
- tick。
- 原始骰值。
- 修改后骰值。
- 修改原因。
- 关联事件 id。
- 是人工改值还是系统改值。

### 9.3 替代方案

- 完全禁止改值：最简单，但不符合 GM 裁决需求。
- 允许任意玩家改值：不安全，会破坏公平。
- GM 人工改值 + 系统自动改值双轨审计：推荐。

### 9.4 选择当前方案的原因

当前方案兼顾了两点：

- 游戏主持需要自由裁决。
- 系统需要可追责和可回放。

## 10. 场景与引擎控制

GM 对场景和引擎应拥有完全控制权。

### 10.1 场景生命周期

建议场景的生命周期管理与权限绑定：

- 创建。
- 加载。
- 激活。
- 暂停。
- 恢复。
- 销毁。

每个状态变化都应该有权限校验，不是任何连接都能触发。

### 10.2 引擎生命周期

引擎启停、实体挂载、实体卸载、冻结恢复都应该由 GM 或等效管理权限触发。

### 10.3 替代方案

- 纯自动化场景管理：不适合人工主持场景。
- 后台单独管理页面：可行，但不如权限系统内聚。
- 场景权限与引擎权限一体化：更符合当前产品定位。

## 11. 前端策略

前端不是安全层，而是权限感知层。

### 11.1 前端应该知道什么

- 当前用户身份。
- 当前角色类型。
- 当前可控实体集。
- 当前可见范围。
- GM 工具是否可用。

### 11.2 前端应该怎么表现

- PL 只看到自己的控制按钮。
- OB 只看到观察入口。
- GM 额外看到授权、场景和日志管理入口。

### 11.3 为什么前端不能做最终判断

因为前端可以被篡改、跳过或模拟。前端的任务只是减少误操作和改善体验，不能代替后端做权限裁决。

### 11.4 替代方案

- 前端不分角色：实现快，但体验差。
- 前端强分角色：体验好，但必须配合后端再次校验。
- 前后端双层一致：推荐。

## 12. 数据持久化

权限系统不能只存在于内存中。

建议持久化：

- 用户与角色。
- 用户与场景关系。
- 用户与实体控制关系。
- 权限授予与撤销记录。
- 会话与 token 版本。
- 审计日志与骰子改值记录。

### 12.1 为什么要持久化

如果权限只在内存里：

- 重启就丢。
- 断线恢复困难。
- 审计无法追溯。

### 12.2 替代方案

- 只用内存：不推荐。
- 只查数据库：高频授权会慢。
- 数据库持久化 + 内存缓存：推荐。

## 13. 测试策略

权限系统必须分层测试。

### 13.1 单元测试

重点覆盖：

- JWT 解析。
- 角色判断。
- capability、scope、condition 的组合规则。
- 日志过滤。
- 骰子审计对象生成。

### 13.2 集成测试

重点覆盖：

- 未登录拒绝。
- 伪造 actorId 拒绝。
- PL 不能控制他人实体。
- GM 可以控制任意实体。
- OB 只能观察。
- 授权撤销后立即失效。

### 13.3 回归测试

重点覆盖：

- 断线重连。
- 场景同步后的 transient state 重建。
- 日志分流正确。
- 骰子改值可追踪。

### 13.4 为什么必须三层都做

权限问题往往不是某个函数写错，而是多层链路组合后才出错。只测一个层面，很容易漏掉会话绑定、授权传播和广播过滤的问题。

## 14. 实施分期

### Phase 0：契约先行

先定义角色、权限域、会话、审计、Intent、错误码和日志可见性的共享契约，再同步到 shared 类型和 Prisma 模型草案。

### Phase 1：认证与会话

先修 Socket 认证、CORS 和会话绑定，确保未认证连接不能发起任何控制行为。

### Phase 2：授权中间件

把场景、引擎、实体、日志、骰子改值、权限委派收口到统一 policy service，并补上权限快照的生成与增量更新机制。

### Phase 3：可见性与日志

完成 StateBroadcaster 与 VisibilityFilter，把 GM/PL/OB 的视图彻底分开，同时把广播改成按组分发而不是按用户逐个过滤。

### Phase 4：前端感知

根据身份和权限重构 HUD、实体列表和 GM 管理入口，但不把安全边界放进前端。

### Phase 5：测试与加固

补齐权限矩阵测试、回归测试、安全测试与拒绝路径测试，再根据结果做性能优化和细节加固。

## 15. 关键文件

- [packages/backend/src/network/SocketServer.ts](packages/backend/src/network/SocketServer.ts)
- [packages/backend/src/network/IntentRouter.ts](packages/backend/src/network/IntentRouter.ts)
- [packages/backend/src/network/StateBroadcaster.ts](packages/backend/src/network/StateBroadcaster.ts)
- [packages/backend/src/network/VisibilityFilter.ts](packages/backend/src/network/VisibilityFilter.ts)
- [packages/backend/src/campaigns/CampaignManager.ts](packages/backend/src/campaigns/CampaignManager.ts)
- [packages/backend/src/campaigns/engines/CombatEngine.ts](packages/backend/src/campaigns/engines/CombatEngine.ts)
- [packages/backend/src/utils/Logger.ts](packages/backend/src/utils/Logger.ts)
- [packages/shared/src/index.ts](packages/shared/src/index.ts)
- [packages/backend/prisma/schema.prisma](packages/backend/prisma/schema.prisma)
- [packages/frontend/src/store/gameStore.ts](packages/frontend/src/store/gameStore.ts)
- [packages/frontend/src/network/socketClient.ts](packages/frontend/src/network/socketClient.ts)
- [packages/frontend/src/ui/HUD.tsx](packages/frontend/src/ui/HUD.tsx)
- [packages/frontend/src/App.tsx](packages/frontend/src/App.tsx)
- [权限矩阵表.md](权限矩阵表.md)
- [接口草案.md](接口草案.md)
- [数据模型草案.md](数据模型草案.md)
- [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md)
- [ISSUES_CONSOLIDATED_REPORT.md](ISSUES_CONSOLIDATED_REPORT.md)
- [ISSUES_QUICK_REFERENCE.md](ISSUES_QUICK_REFERENCE.md)
- [packages/backend/BACKEND_REVIEW_REPORT.md](packages/backend/BACKEND_REVIEW_REPORT.md)
- [packages/frontend/FRONTEND_REVIEW_REPORT_2026-04-30.md](packages/frontend/FRONTEND_REVIEW_REPORT_2026-04-30.md)

## 16. 为什么选择当前方案

当前方案不是“最短路径”，而是“最稳路径”。它之所以适合这个项目，是因为它同时满足了五个条件：

1. 能表达 GM 的全局管理权。
2. 能表达 PL 的默认自有控制权。
3. 能表达 OB 的独立观察语义。
4. 能把日志、骰子和权限委派纳入同一条审计链。
5. 能随着项目成熟逐步扩展，而不是一开始就把系统做死。

如果只追求快，可以把权限逻辑写进前端按钮里；如果只追求灵活，可以一上来做极复杂的 ABAC；但前者不安全，后者不可维护。当前方案选择的是中间点：足够严格、足够可扩展、足够贴合当前代码库阶段。

## 17. 需要继续细化的内容 (包含挂起问题)

* **挂起记录 (2026-05-05)**: 动态 ABAC 实体委托已通过 `PermissionGrantRepository` 实现，但是以下两个高级权限特性延后处理，并记录在项目中待日后解决：
  1. **底层审计表与骰子改值防篡改 (Audit & Dice Overrides)**: 尚未落地至 `AuditEventRepository` 和引擎。
  2. **引擎生命周期控制 (Engine Lifecycle Control)**: 场景启停、引擎冻结由于配套的前端 GM 界面未完成，API 暂不提供。

下一步最值得继续维护和同步的三份文稿已经拆出，后续如果需求变化，只需要在这些文稿里继续增补：

1. [权限矩阵表.md](权限矩阵表.md)：把 GM、PL、OB 在场景、引擎、实体、日志、骰子、授权六个域下的允许项逐条列出来。
2. [接口草案.md](接口草案.md)：把认证、授权、日志过滤、授权变更、骰子改值这些接口的输入输出和错误码定义清楚。
3. [数据模型草案.md](数据模型草案.md)：把用户、角色、授权、会话、审计记录、日志订阅等表结构先定下来。

这三份补齐后，就可以从“设计文稿”直接进入“开发任务拆解”。
