# 权限矩阵表

## 1. 术语定义

- PL：Player，指“玩家主体”的权限视角。
- PC：Player Character，指由 PL 所操控的角色或对象。
- GM：Game Master，指拥有全局裁决权和管理权的主持主体。
- OB：Observer，指独立的观察者主体，默认只读。

本文件中的“玩家权限”全部使用 PL 表达，避免与 PC（被操控对象）混淆。

## 2. 设计目标

权限矩阵的作用不是简单列出“能不能点按钮”，而是把系统中所有高风险能力按主体、资源域、动作类型和审计要求统一描述出来。这样做的目标有四个：

1. 让 GM 的全域管理权有明确落点。
2. 让 PL 的默认权限足够清晰，能表达“只控制自己的 PC”。
3. 让 OB 的只读语义独立于 PL，避免和普通玩家混用。
4. 让后端、前端、日志和审计使用同一套判定结果。

## 3. 权限总原则

- GM 永远优先于其他规则。
- PL 默认只能控制自己的 PC，不能控制其他角色。
- OB 默认只读，不参与控制链路。
- 临时授权必须带作用域和过期条件。
- 所有高风险动作必须记录审计。

## 4. 权限矩阵总表

| 主体 | 场景域 | 引擎域 | 实体域 | 日志域 | 骰子域 | 权限域 |
|---|---|---|---|---|---|---|
| GM | 全部允许 | 全部允许 | 全部允许 | 全部允许 | 全部允许 | 全部允许 |
| PL | 仅加入被允许场景 | 仅对自己可见的场景级操作 | 仅控制自己的 PC 或被授权实体 | 仅读取可见日志 | 默认不可人工改值 | 仅查看自己的授权状态 |
| OB | 仅加入观战场景 | 默认禁止 | 默认禁止 | 仅读取被允许的观察日志 | 默认禁止 | 默认禁止 |

## 5. 分域细则

### 5.1 场景域

#### GM
- 创建场景。
- 销毁场景。
- 暂停与恢复场景。
- 调整场景可见性与访问范围。
- 强制将任意主体拉入或移出场景。

#### PL
- 加入自己被允许的场景。
- 退出当前场景。
- 在被授权时进入指定观战场景。
- 不能无授权切换到其他场景。

#### OB
- 仅能进入被允许的观战入口。
- 不能创建、销毁或管理场景。
- 不能主动切换到未授权场景。

#### 备选方案
- 仅用房间码控制场景进入。
- 用场景 ACL 管理访问。
- 由 GM 手工发放一次性票据。

#### 选择当前方案的原因
房间码只能解决“谁能进”，不能解决“进来后能做什么、能看到什么”。ACL 和票据都可以作为补充，但最终仍需要统一权限判断。

### 5.2 引擎域

#### GM
- 启动与停止引擎。
- 挂载与卸载实体。
- 冻结、恢复、重置引擎状态。
- 强制处理异常战斗状态。

#### PL
- 默认不能直接启停引擎。
- 仅能触发与自己 PC 相关的意图。
- 在被授权时，可执行局部管理动作。

#### OB
- 默认不能影响引擎状态。

#### 备选方案
- 引擎自动根据场景生命周期启动。
- 引擎由独立管理后台控制。
- 引擎操作统一暴露为 GM 命令。

#### 选择当前方案的原因
引擎是规则执行器，必须保留 GM 人工裁决能力；完全自动化会丢失主持控制，完全后台化则会切断与玩法流程的耦合。

### 5.3 实体域

#### GM
- 控制任何实体，无论生死。
- 查看任意实体的完整状态。
- 接管、转移、冻结或删除实体控制权。
- 查看隐藏实体和被脱敏实体。

#### PL
- 默认只控制自己的 PC。
- 可以查看自己可见的实体。
- 在 GM 授权后可操作额外实体。
- 不能默认控制其他 PL 的 PC。

#### OB
- 默认不能控制实体。
- 可以查看被允许观察的实体集合。

#### 备选方案
- 只按当前选中实体决定权限。
- 只按 owned entity 列表决定权限。
- 由 GM 动态接管任意实体。

#### 选择当前方案的原因
当前选中态只是 UI 状态，不能作为安全边界；owned entity 能表达默认控制权，但无法表达临时授权；因此需要“默认归属 + 临时授权 + GM 全域覆盖”的组合模型。

### 5.4 日志域

#### GM
- 读取全部日志。
- 读取审计日志。
- 读取调试日志。
- 导出全量日志。

#### PL
- 读取自己的可见日志。
- 读取与自己 PC 相关的战斗日志。
- 不能读取默认隐藏给 GM 的日志。

#### OB
- 仅读取被允许的观察日志。
- 不应默认读取战斗全量细节。

#### 备选方案
- 所有人看到同一套日志。
- 仅按日志级别区分，不按主体区分。
- 前端自行隐藏敏感日志。

#### 选择当前方案的原因
日志是高敏感信息，前端隐藏无法防泄漏。必须由服务端按主体和场景过滤后再下发。

### 5.5 骰子域

#### GM
- 查看全部掷骰细节。
- 人工修改骰子结果。
- 批量裁决掷骰事件。
- 为改值写审计原因。

#### PL
- 查看与自己相关的骰子结果。
- 提交重掷请求。
- 默认不能人工改值。

#### OB
- 默认不能改值。
- 仅查看被允许的结果摘要。

#### 备选方案
- 完全禁止改值。
- 允许所有主体改值。
- 只允许系统自动改值。

#### 选择当前方案的原因
完全禁止改值不适合主持场景；允许所有主体改值会破坏公平；只有 GM 人工改值 + 系统自动改值双轨审计，才能兼顾可玩性与可追责性。

### 5.6 权限域

#### GM
- 授予、撤销、转移、临时委托所有权限。
- 管理角色模板。
- 管理主体与场景的绑定关系。

#### PL
- 查看自己的授权状态。
- 接收 GM 发放的 scoped permission。
- 不能管理其他主体权限。

#### OB
- 默认不参与权限管理。

#### 备选方案
- 只允许 GM 管理。
- 允许会话房主管理局部权限。
- 允许角色自助申请授权。

#### 选择当前方案的原因
第一版应先保证 GM 是唯一全局授权源，后续再根据产品需求引入房主或代理管理，不建议一开始就引入多层委托。

## 6. 典型权限场景

### 场景 A：PL 操作自己的 PC
- 合法。
- 需要校验当前会话是否绑定该 PC。
- 需要校验该 PC 是否处于可操作状态。

### 场景 B：PL 操作其他人的 PC
- 默认拒绝。
- 仅在 GM 显式授权后允许。
- 需要记录临时授权来源。

### 场景 C：OB 尝试施法
- 默认拒绝。
- 只有在 GM 临时提升权限后才允许。

### 场景 D：GM 修改骰子结果
- 允许。
- 必须写入审计日志。

### 场景 E：PL 读取战斗日志
- 只允许读取可见范围内的内容。
- 不能读取 GM 专属调试与审计内容。

## 7. 实施优先级

1. 先实现 GM / PL / OB 主体模型。
2. 再实现场景、引擎、实体三大操作域。
3. 再实现日志过滤与骰子审计。
4. 最后实现权限管理和委托。

## 8. 与后续文稿的关系

- [接口草案.md](接口草案.md) 负责描述认证、授权、日志和审计接口。
- [数据模型草案.md](数据模型草案.md) 负责描述数据库实体、关系和索引。
- [权限系统架构与开发计划.md](权限系统架构与开发计划.md) 负责描述总架构、取舍和实施阶段。

## 9. 结论

这个矩阵的核心不是“更多权限”，而是“更清晰的边界”。

- GM 是裁决者。
- PL 是玩家主体。
- PC 是 PL 所操控的对象。
- OB 是观察主体。

只要这四个概念不混淆，后续的接口、数据库和前端 UI 才能稳定落地。