# 接口草案

## 1. 术语说明

- PL：玩家主体。
- PC：PL 所操控的对象。
- GM：最高权限的主持主体。
- OB：默认只读的观察主体。

本草案所有“玩家权限”均指 PL 层，不再把 PC 当作玩家权限主体。

## 2. 接口设计目标

接口设计的目标不是把所有逻辑都塞进 HTTP 或 Socket 消息里，而是把权限链路拆成可验证的几层：

1. 认证接口确认身份。
2. 授权接口确认范围。
3. 广播接口确认可见性。
4. 审计接口确认可追溯。

接口层必须服务于服务端权威，而不是让前端自己决定是否有效。

授权执行不应采用可执行 DSL 字符串，而应由服务端统一的 Policy Service 以策略函数方式判断。数据库只保存角色、授权、范围、过期时间和快照结果，不保存可执行策略文本。

## 3. 身份认证接口

### 3.1 POST /auth/login

用途：账号或房间票据登录，获取 access token。

请求示例：
```json
{
  "accountName": "alice",
  "password": "***"
}
```

响应示例：
```json
{
  "accessToken": "jwt...",
  "refreshToken": "refresh...",
  "expiresIn": 3600,
  "subject": {
    "userId": "user_123",
    "role": "PL",
    "displayName": "Alice"
  }
}
```

### 3.2 POST /auth/refresh

用途：刷新短期 access token。

请求示例：
```json
{
  "refreshToken": "refresh..."
}
```

响应示例：
```json
{
  "accessToken": "jwt...",
  "expiresIn": 3600
}
```

### 3.3 POST /auth/logout

用途：注销会话，回收 token 或提升 token 版本号使其失效。

## 4. Socket 认证接口

### 4.1 AUTHENTICATE

用途：在 WebSocket 连接建立后绑定会话身份。

请求示例：
```json
{
  "token": "jwt..."
}
```

成功响应：
```json
{
  "ok": true,
  "subject": {
    "userId": "user_123",
    "role": "PL",
    "controlScope": ["pc_1"],
    "sessionId": "sess_456",
    "permissionSnapshotVersion": 12
  }
}
```

失败响应：
```json
{
  "ok": false,
  "code": "AUTH_FAILED",
  "message": "认证失败"
}
```

### 4.2 JOIN_SCENE

用途：加入场景。

请求示例：
```json
{
  "sceneId": "room_1"
}
```

说明：
- 不再允许客户端自报 actorId 作为可信控制依据。
- 服务端应根据 subject 与授权关系决定可进入的场景和可见范围。

### 4.3 LEAVE_SCENE

用途：离开当前场景。

### 4.4 CLIENT_INTENT

用途：发送控制意图。

请求示例：
```json
{
  "intentType": "MOVE",
  "targetEntityId": "pc_1",
  "payload": {
    "targetCoords": { "x": 10, "y": 20, "z": 0 }
  }
}
```

说明：
- targetEntityId 必须经过服务端授权确认。
- 客户端不能只凭自己声明就控制任意 PC。
- GM 可通过授权上下文获得全域控制能力。

建议统一所有控制请求的最小形态，便于 Socket 与 HTTP 共享同一套权限判断：

```ts
interface Intent {
  type: string;
  actor?: string;
  target?: string;
  payload: unknown;
}
```

服务端应先校验 intent 结构，再交给 Policy Service 和权限快照判定，最后才进入引擎执行。

## 5. 授权接口

### 5.1 GET /permissions/me

用途：查询当前主体的权限摘要。

响应示例：
```json
{
  "role": "PL",
  "allowedScenes": ["room_1"],
  "ownedEntities": ["pc_1"],
  "delegatedEntities": ["ally_2"],
  "capabilities": ["read_own_log", "move_own_pc"],
  "visibleEntities": ["pc_1", "ally_2"],
  "permissionSnapshotVersion": 12,
  "expiresAt": "2026-05-01T12:00:00Z"
}
```

说明：
- 该接口返回的是当前权限摘要和快照版本，不是可执行策略。
- IntentRouter 可以直接消费快照结果做高频判定。
- 权限变更后快照版本应递增，过期快照必须重新签发。

### 5.2 POST /permissions/grant

用途：GM 授权某个主体在某个范围内执行某项能力。

请求示例：
```json
{
  "subjectId": "user_123",
  "capability": "control_entity",
  "scope": "entity:ally_2",
  "expiresAt": "2026-05-01T12:00:00Z",
  "reason": "临时代管"
}
```

### 5.3 POST /permissions/revoke

用途：撤销授权。

### 5.4 GET /permissions/audit

用途：查询授权变更审计。

说明：默认只对 GM 和审计角色开放。

### 5.5 GET /permissions/snapshot

用途：获取当前会话的权限快照。

响应示例：
```json
{
  "sessionId": "sess_456",
  "userId": "user_123",
  "role": "PL",
  "version": 12,
  "controllableEntities": ["pc_1"],
  "visibleEntities": ["pc_1", "ally_2"],
  "capabilities": ["move_own_pc", "read_own_log"],
  "currentSceneIds": ["room_1"],
  "issuedAt": "2026-05-01T11:30:00Z",
  "expiresAt": "2026-05-01T12:00:00Z"
}
```

说明：
- 快照用于高频 intent 判定和广播分组。
- 快照是可缓存的判定结果，不是唯一真相。
- 授权变更、会话刷新、重连恢复后都应重新生成快照。

## 6. 场景与引擎接口

### 6.1 POST /scenes/:sceneId/start

用途：启动场景或场景引擎。

权限：GM。

### 6.2 POST /scenes/:sceneId/pause

用途：暂停场景。

权限：GM。

### 6.3 POST /scenes/:sceneId/resume

用途：恢复场景。

权限：GM。

### 6.4 POST /scenes/:sceneId/destroy

用途：销毁场景。

权限：GM。

## 7. 日志接口

### 7.1 GET /logs

用途：按主体、场景、级别、可见性筛选日志。

查询参数示例：
- sceneId
- visibility
- level
- beforeTick
- afterTick

说明：
- GM 可以拿到全量日志。
- PL 只能看到与自己相关的日志。
- OB 只能看到被允许的观察日志。

说明补充：
- 服务端广播前应按身份和可见性分组，再向组广播。
- 前端不应收到不属于自己的日志后再自行过滤。

### 7.2 GET /logs/audit

用途：读取审计日志。

权限：默认仅 GM。

说明：审计日志应按追加方式写入，不应提供删除或覆盖接口。

## 8. 骰子接口

### 8.1 GET /dice/:rollId

用途：查询某次掷骰结果。

### 8.2 POST /dice/:rollId/override

用途：修改骰子结果。

权限：默认 GM。

请求示例：
```json
{
  "newValue": 18,
  "reason": "主持裁决"
}
```

### 8.3 POST /dice/:rollId/reroll

用途：请求重掷。

权限：按规则和主体授权决定。

## 9. 审计接口

### 9.1 GET /audit/events

用途：获取授权、改值、场景控制等审计事件。

### 9.2 POST /audit/mark

用途：为某条审计记录补充 GM 说明或裁决原因。

## 10. 错误码建议

| 错误码 | 含义 | 典型场景 |
|---|---|---|
| UNAUTHENTICATED | 未认证 | token 缺失或无效 |
| UNAUTHORIZED | 无权限 | PL 试图控制别人的 PC |
| INVALID_TARGET | 目标不合法 | 目标实体不在作用域内 |
| PERMISSION_EXPIRED | 授权已过期 | 快照或授权过期 |
| RATE_LIMITED | 请求过于频繁 | 高频意图提交 |
| AUDIT_REQUIRED | 需要审计 | 改骰或授权变更 |
| NOT_IN_SCENE | 未加入场景 | 发起意图前未进入房间 |
| RESOURCE_FORBIDDEN | 资源被禁止访问 | 读取不该看的日志 |
| INVALID_SCOPE | 授权范围无效 | 授权过期或不匹配 |

说明：
- AUTH_FAILED 可视为 UNAUTHENTICATED 的兼容别名。
- NOT_AUTHORIZED 可视为 UNAUTHORIZED 的兼容别名。

## 11. 设计取舍

### 11.1 为什么接口层要显式区分认证和授权

因为认证回答“你是谁”，授权回答“你能做什么”。如果把两者塞到一个接口里，后续的会话续期、权限刷新、日志审计都会变难。

### 11.2 为什么 Socket 和 HTTP 都要有接口

- HTTP 适合管理类、查询类、配置类操作。
- Socket 适合实时意图和状态同步。

两者分开后，系统边界更清楚，也更容易做限流和权限收口。

### 11.3 为什么 GM 的能力不能只靠前端按钮

因为前端按钮会被篡改。GM 的能力必须由服务端接口兜底，前端只是入口，不是裁判。

## 12. 与后续文稿的关系

- [权限矩阵表.md](权限矩阵表.md) 决定谁能调用哪些接口。
- [数据模型草案.md](数据模型草案.md) 决定接口依赖哪些表和关系。
- [权限系统架构与开发计划.md](权限系统架构与开发计划.md) 决定整体实施顺序。

## 13. 结论

接口层应该被设计成“认证入口 + 授权闸门 + 审计出口”的组合，而不是单纯的业务 API 列表。这样才能把 PL 和 PC 的边界、GM 的全域权限、OB 的只读语义真正落到可执行层面。